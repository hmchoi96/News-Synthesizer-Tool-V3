services:
  - type: web
    name: wiserbond-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        value: your_openai_api_key_here
